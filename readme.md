### Example of Interrupt driven Sprites

Loads a bitmap from memory and displays on screen as a hardware sprite.

Loads bitmap sequence for animating sprite frames.

Diamond animation is triggerd by interrupt timer.

Little man is moved in main lop of program.

Press ESC to exit the program.
